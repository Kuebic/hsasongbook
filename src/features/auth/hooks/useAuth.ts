/**
 * useAuth Hook
 *
 * Re-exports the useAuth hook from AuthProvider for convenience.
 * This maintains consistency with the feature module structure.
 *
 * Phase 5: Authentication Flow
 */

export { useAuth } from '../components/AuthProvider';
